# Antigravity 2.0 (Naver SaaS) - 상세 아키텍처 및 시스템 뼈대

> **문서 목적**: 코딩을 모르는 기획자/비개발자도 전체 시스템이 어떻게 맞물려 돌아가는지 이해하고, 향후 개발자(또는 AI)에게 명확한 지시(예: "Zone 3의 Evaluator만 수정해")를 내릴 수 있도록 돕는 지도(Map)입니다.

---

## 🗺️ [ZONE-1] 디렉토리 트리 (Directory Tree)
프로젝트의 전체 구조입니다. 이 폴더들이 각각의 역할을 담당합니다.

- `src/app`: 사용자에게 보여지는 웹 페이지 화면들 (프론트엔드 UI)
  - `dashboard/`: 로그인 후 진입하는 메인 작업 공간 (글쓰기, 보관함 등)
  - `api/`: 눈에 보이지 않는 뒷단(백엔드) 서버 로직 (AI 호출, 결제 처리 등)
- `src/components`: 버튼, 입력창 등 여러 화면에서 재사용되는 UI 레고 블록들
- `src/extension`: 네이버 블로그에 글을 자동으로 붙여넣어주는 크롬 익스텐션 프로그램
- `prisma/`: 데이터베이스(DB) 설계도 (테이블 구조 정의)

---

## 🎨 [ZONE-2] 프론트엔드 (Frontend UI)
사용자가 마우스를 클릭하고 화면을 보는 영역입니다.

- **기술 스택**: Next.js (App Router), Tailwind CSS
- **상태 관리(State)**: 글쓰기 화면(`write/page.tsx`)에서 사용자가 키워드를 입력하고 '생성'을 누르면, 프론트엔드는 서버(Zone 3)에 일을 시킵니다. 서버가 글을 쓰는 동안 프론트엔드는 **"지금 다 썼어?"(Polling)** 라며 3~5초마다 계속 물어보며 진행률(자료 수집 -> 기획 -> 초안 -> 검수)을 화면에 표시합니다.
- **특징**: AI가 생성한 완성된 글(HTML)을 그대로 화면에 미리보기로 보여줍니다.

---

## 🧠 [ZONE-3] 백엔드 및 AI 파이프라인 (Backend / AI Flow)
가장 중요한 핵심 뇌입니다. 외부 자동화 툴(n8n, Zapier) 없이 자체적으로 돌아가는 **3단계 릴레이 파이프라인**을 가지고 있습니다.

1. **`/api/generate-seo`**: 사용자가 글쓰기를 누르면 가장 먼저 실행됩니다. 검색엔진(Zone 10)에서 자료를 수집한 뒤, 릴레이 바톤을 넘깁니다. (상태: `SEARCHING`)
2. **`worker/planner` (기획자)**: 수집된 자료를 바탕으로 글의 목차와 기승전결 뼈대를 잡습니다. (상태: `PLANNING`)
3. **`worker/generator` (작가)**: 기획안을 바탕으로 실제 살을 붙여서 완벽한 HTML 블로그 글(초안)을 작성합니다. (상태: `GENERATING`)
4. **`worker/evaluator` (검수자)**: 작성된 글에 과대광고나 금지어가 없는지, 광고법 위반 여부를 검사하고 제목을 뽑아냅니다. (상태: `EVALUATING` -> `COMPLETED`)

이 릴레이는 **Upstash QStash**라는 메시지 큐(우체부)를 통해 이루어지며, 각 단계가 끝날 때마다 다음 단계로 데이터를(JSON) 전달합니다.

---

## 🤖 [ZONE-4] 클라이언트 RPA 자동화 (Chrome Extension)
완성된 글을 네이버 블로그에 자동으로 올리는 크롬 확장 프로그램입니다.

- **위치**: `src/extension/` (background.js, content.js)
- **작동 원리**: 네이버 블로그 서버를 직접 해킹(스크래핑)하는 위험한 방식이 아닙니다. 대신, 사용자의 브라우저(크롬)에서 직접 네이버 스마트에디터를 조종하여, 사람이 복사/붙여넣기 하는 것과 똑같은 방식으로 화면(DOM)에 HTML을 주입합니다. (ProseMirror 에디터 동기화 활용)

---

## 💾 [ZONE-5] 데이터베이스 (Database)
데이터를 저장하는 창고입니다. (PostgreSQL, Prisma 사용)

- **`User` (사용자)**: 이메일, 요금제(plan_type), 남은 크레딧 개수
- **`Profile` (프로필)**: 업체명, 연락처, 주소 등 블로그 글에 들어갈 기본 정보
- **`Project` (프로젝트)**: 글을 묶어서 관리하는 폴더 역할
- **`Article` (글)**: 실제 생성된 블로그 글 (HTML, Markdown, 검색된 참고자료 등)
- **`ApiKey` (API 키)**: 쿠팡 파트너스, 워드프레스, 티스토리 연동 키 저장소
- **`PaymentHistory` (결제내역)**: 사용자가 결제한 영수증 내역

---

## ⚙️ [ZONE-6] 글쓰기 코어 엔진 (Writing Engine)
AI 모델과 소통하는 엔진입니다.

- **기술**: Vercel AI SDK
- **모델 분리**: 사용자의 요금제(Zone 9)에 따라 똑똑한 AI를 차등 적용합니다. (Premium은 Claude 3.5 Sonnet, Free/Pro는 GPT 모델 등)
- **방식**: 실시간 타자치는 효과(Streaming)를 쓰지 않고, 뒷단에서 조용히 `generateText`와 `generateObject`(형식에 맞춰 결과만 뽑아냄)로 글을 뭉텅이로 완성한 뒤 한 번에 프론트엔드로 보내줍니다.

---

## ☁️ [ZONE-7] Vercel 배포 및 인프라 (Deployment & Infra)
서버를 띄우는 클라우드 환경입니다.

- **서버리스 한계 극복**: Vercel은 60초가 지나면 서버를 강제로 꺼버립니다(`maxDuration = 60`). AI가 글을 쓰는데 60초 이상 걸리기 때문에, Zone 3에서 설명한 **QStash**를 이용해 60초짜리 작업 3개(기획->작성->검수)로 쪼개서 릴레이를 돌리는 천재적인 설계를 가지고 있습니다.
- **캐시 (Vercel KV)**: DB까지 가지 않고 빠르게 현재 글쓰기 진행 상태를 저장하고 읽기 위해 임시 메모리(Redis/KV)를 사용합니다.

---

## 🔐 [ZONE-8] 회원가입 및 권한 관리 (Auth & Security)
- **기술**: Supabase Auth
- 로그인/회원가입 기능을 담당하며, 프론트엔드와 백엔드 모두에서 이 사람이 진짜 결제한 유저가 맞는지 깐깐하게 체크합니다.

---

## 💳 [ZONE-9] 결제 및 과금 (Billing & Monetization)
- **기술**: 토스페이먼츠(Toss) 및 포트원(PortOne) 혼용
- 유저가 결제를 하면 `PaymentHistory`에 기록을 남기고, `User` 테이블의 `plan_type`(Pro/Premium)과 `credits`(글쓰기 티켓)을 늘려줍니다. 글을 1번 생성할 때마다 크레딧이 1씩 깎입니다.

---

## 🔍 [ZONE-10] 외부 API 및 검색 (3rd-Party & Search)
- **Tavily API**: AI가 환각(거짓말)을 하지 않도록, 글을 쓰기 직전에 구글/네이버에서 최신 정보를 검색해서 팩트체크용 자료로 건네주는 역할을 합니다.

---
---

## ➕ [발견된 추가 ZONE]

코드를 스캔해본 결과, 대표님이 말씀하신 10가지 외에 **2가지의 핵심 모듈**이 더 발견되었습니다.

### 🌐 [ZONE-11] 외부 블로그 멀티 퍼블리싱 (Multi-Publishing)
네이버 뿐만 아니라 다른 블로그에도 동시에 글을 발행하는 기능이 내장되어 있습니다.
- `ApiKey` DB 테이블을 보면 워드프레스(WordPress)와 티스토리(Tistory)의 아이디/비밀번호(토큰)를 저장하는 칸이 있습니다.
- `src/app/api/publish` 등에서 워드프레스/티스토리 API를 찔러서 외부로 원격 전송하는 로직이 존재합니다.

### 💰 [ZONE-12] 제휴 마케팅 자동화 (Affiliate Marketing)
- `ApiKey` 테이블에 `coupang_access_key`가 존재하고, `Article`에 `coupang_links` 정보가 저장됩니다.
- 글을 작성할 때 쿠팡 파트너스 링크를 자동으로 생성하여 블로그 글 중간에 삽입해 수익을 창출하는 로직이 포함되어 있습니다.
