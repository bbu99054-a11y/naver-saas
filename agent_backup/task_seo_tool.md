# Free SEO Diagnostic Tool Tasks

- [x] 1. Create `src/app/seo-check/page.tsx` with the UI for URL input, simulated loading, and scary result dashboard.
- [x] 2. Update `src/app/page.tsx` (Landing Page) to include a link to the `/seo-check` page in the Navbar.
- [x] 3. Update `src/app/blog/page.tsx` and `src/app/blog/[slug]/page.tsx` to include the link in the Navbar as well.
- [x] 4. Test compilation with `tsc --noEmit` and push to GitHub.
