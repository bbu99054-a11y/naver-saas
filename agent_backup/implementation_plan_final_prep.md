# 🚀 런칭 직전 파이널 보수 공사 기획안

대표님, 대대적인 홍보와 트래픽 유입을 대비하여 필수 기능들을 보강하는 파이널 패치 플랜입니다. 아래 기획안을 확인해주시고 승인해 주시면 즉시 시공에 들어가겠습니다!

## User Review Required & Open Questions
> [!IMPORTANT]
> 1. **채널톡(ChannelTalk) 플러그인 키**: 혹시 채널톡 회원가입 후 발급받으신 `pluginKey`가 있으신가요? 없으시다면 임시 더미 키로 껍데기만 연동해두겠습니다.
> 2. **이용약관/개인정보처리방침 내용**: 표준 템플릿(B2B SaaS용)을 사용하여 더미 페이지를 우선 만들어 두겠습니다. 추후 실제 회사 정보로 텍스트만 교체하시면 됩니다. 괜찮으신가요?
> 
> **위 두 가지 사항에 대해 답변과 함께 우측 하단 `Proceed(승인)`를 눌러주시면 개발을 시작하겠습니다.**

---

## 1. 챗봇 (채널톡) 연동 💬
고객 문의 응대 및 리드 수집을 위해 채널톡 위젯을 전역에 설치합니다.
- **[수정]** `src/app/layout.tsx` 또는 `dashboard/layout.tsx`에 채널톡 Boot Script(바닐라 JS) 삽입
- 사용자 로그인 정보(이름, 이메일)를 채널톡에 동기화하여 CS 효율 극대화

## 2. 법적 필수 페이지 (약관) 구축 📄
- **[신규]** `src/app/privacy/page.tsx` (개인정보처리방침)
- **[신규]** `src/app/terms/page.tsx` (이용약관)
- **[수정]** 로그인 페이지(`login/page.tsx`) 하단 및 대시보드 하단에 해당 링크 추가

## 3. 미리보기 에디터 직접 수정 기능 ✏️
현재 읽기 전용인 HTML 뷰어를 사용자가 텍스트를 직접 치고 지울 수 있는 형태(WYSIWYG)로 개편합니다.
- **[수정]** `src/app/dashboard/write/page.tsx`
  - 뷰어 `<div>`에 `contentEditable={true}` 속성 부여
  - `id="editor-preview"`를 할당하여 브라우저 DOM 객체로 직접 접근
- **[수정]** `src/components/CopyToNaverBtn.tsx` 등 복사 버튼들
  - 기존의 고정된 상태(State)를 복사하는 방식에서, 사용자가 수정한 뷰어 DOM(`editor-preview`)의 최종 `innerHTML`을 긁어가도록 로직 수정

## 4. 사용 설명서 (노션 연결) 📖
- **[수정]** `src/app/dashboard/layout.tsx`
  - 현재 `#`으로 비어있는 [📖 사용 가이드] 버튼에 임시 가이드 링크 연결 (추후 대표님의 Notion 가이드 URL로 쉽게 교체 가능하도록 주석 처리)

## 5. 외부 API 설정 화면 (워드프레스/티스토리) ⚙️
- **[신규/수정]** `src/app/dashboard/settings/page.tsx`
  - 네이버 검색 API(기존) 외에 **[워드프레스 API 설정]** 및 **[티스토리 토큰 설정]** UI 폼 추가
  - 입력한 키가 `ApiKey` DB 테이블에 안전하게 저장/조회되도록 백엔드 연결

## 🏁 검증 계획 (Verification Plan)
- [x] 글을 생성한 뒤 화면에서 글자를 직접 지우거나 추가하고 복사 버튼을 눌렀을 때 수정한 내용이 복사되는지 테스트
- [x] 채널톡 아이콘이 화면 우측 하단에 정상적으로 노출되는지 테스트
- [x] 워드프레스/티스토리 API 키를 설정창에서 저장하고 새로고침해도 남아있는지 DB 연동 테스트
