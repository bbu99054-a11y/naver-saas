# AGENTS.md 상위 1% 3단계 통제 체계(Never/Ask First/Always) 업그레이드 완료 보고서

## 🎯 작업 개요
- **작업 내용**: `AGENTS.md` 지침을 전 세계 상위 1% 개발팀의 최신 표준인 **`3단계 권한 통제 체계 (🔴 절대 금지 / 🟡 사전 승인 / 🟢 항상 실행)`**로 전면 업그레이드 완료.
- **보강된 안전장치**:
  1. 보안 키/시크릿(API Key, DB 주소, 결제 키) 하드코딩 및 깃 노출 절대 금지
  2. 코드 날림 및 에러 은폐(기능 삭제, any 남발, 에러 주석 처리) 절대 금지
  3. 무분별한 외부 라이브러리(`npm install`) 추가 통제
  4. 데이터베이스 파괴적 변경(마이그레이션 reset, 테이블 드롭) 사전 승인 의무화
- **관련 파일**: [AGENTS.md](file:///c:/workspace/naver_SaaS_Copy_For_USB/AGENTS.md)
- **검증 결과**: TypeScript 타입 무결성 검증(`npx tsc --noEmit`) 에러 0건 통과 완료.

---

## 🛠️ 업그레이드된 마스터 헌법 구조

### 1. 기본 원칙 및 시스템 맥락 (Core Context)
- 시간적 맥락 (2026년 기준)
- 엄격한 환각 방지 (거짓말 금지)
- 배포 맥락 유지 (Vercel 실시간 배포 및 자율 검증)
- 일반 맥락 유지 (작업 집중)
- 아키텍처 구역 격리 (11개 Zone 준수 및 Zone 12 제휴마케팅 부활 영구 금지)

### 2. 🔴 절대 금지 (Never - 위반 시 즉시 중단)
1. **보안 키 및 시크릿 노출 절대 금지:** `.env` 환경 변수 격리 의무
2. **자동 깃 푸시 금지:** 대표님 명시적 지시 시에만 실행
3. **코드 날림 및 에러 은폐 금지:** 에러 회피 목적 기능 삭제, `any` 날림, 주석 은폐 금지
4. **코드 임의 전면 덮어쓰기 금지:** 정밀 수술형(Surgical) 최소 수정 원칙
5. **폐기된 시스템 부활 금지:** Zone 12 및 n8n/Make 재도입 금지

### 3. 🟡 사전 승인 필수 (Ask First - 대표님 승인 후 실행)
1. **코드 수정 전 구현 계획서 승인 의무:** 비개발자 CEO 눈높이 6대 표준 양식 준수
2. **외부 라이브러리(`npm install`) 신규 추가 시:** 필요성 사전 소명 및 승인
3. **데이터베이스 스키마 및 파괴적 변경 시:** 사전 백업 및 승인

### 4. 🟢 항상 실행 (Always - 지시 없이도 자율 수행)
1. **선(先) 조사, 후(後) 코딩:** 기존 파일 열람 및 흐름 완벽 파악
2. **자율 빌드 검증:** 완료 선언 전 타입/빌드 에러 0건 스스로 확인
3. **비개발자 CEO 관점 설명:** 전문 용어 배제 및 직관적 비유 활용
4. **워크스루 보고서 저장 위치 규칙:** `walkthrough/오늘날짜/walkthrough_시간/` 자동 보관
5. **크몽 주문 처리 프로토콜 상시 대기:** 8종 벤토 인포그래픽 카드 제작 매뉴얼 유지

---

## 🧪 검증 결과
- **TypeScript 타입 체크**: `cmd.exe /c npx tsc --noEmit` 실행 결과 에러 0건 (성공)
- **문서 무결성**: 마크다운 렌더링 및 Next.js 시스템 마커(1~9번 줄) 보존 확인 완료
